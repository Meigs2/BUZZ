<Window x:Class="BUZZ.Core.CharacterManagement.CharacterManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="BuzzCharacterManagementWindow" Height="500" Width="350">
    <Grid>
        <DockPanel>
            <Grid DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button x:Name="AddCharacterButton" Content="Add BuzzCharacter" VerticalAlignment="Bottom" Margin="5" Grid.Column="0" Click="AddCharacterButton_Click"/>
                <Button x:Name="RemoveCharacterButton" Content="Remove BuzzCharacters" VerticalAlignment="Bottom" Margin="5" Grid.Column="1"/>
            </Grid>
        </DockPanel>
    </Grid>
</Window>
